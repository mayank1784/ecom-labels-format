import SigningCard from "../components/SigningCard"

const Login = () => {
    return (
        <div className="login">
            <SigningCard type="login" />
        </div>
    )
}

export default Login